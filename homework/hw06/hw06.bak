; ;;;;;;;;;;;;;;
; ; Questions ;;
; ;;;;;;;;;;;;;;
; Scheme
(define (cddr s) (cdr (cdr s)))

(define (cadr s) 
    (car (cdr s)))

(define (caddr s) 
    (car (cdr (cdr s)))

(define (sign x) 'YOUR-CODE-HERE)

(define (square x) (* x x))

(define (pow b n) 'YOUR-CODE-HERE)

(define (unique s) 'YOUR-CODE-HERE)
